import React from "react";
import UpdateButton from "./updateButton";
import {
  render,
  fireEvent,
  cleanup,
  waitForElement
} from "@testing-library/react";

afterEach(cleanup);
it("Updates the selected bus stops", async () => {
  //   expect(1).toMatchSnapshot();
});
